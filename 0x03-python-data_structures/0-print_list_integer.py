#!/usr/bin/python3
import string
def print_list_integer(my_list=[]):
    """This function prints a list of integer
       Arguments:
                 my_list: the list of integer
    """
    for i in my_list:
        print(str.format(i))
