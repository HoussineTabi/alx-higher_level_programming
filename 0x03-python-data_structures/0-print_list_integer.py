#!/usr/bin/python3
import string


def print_list_integer(my_list=[]):
    """This function is prints a list of integer
       Argmuents:
                 my_list: the list that contains the integes
    """
    for i in my_list:
        print(str.format(i))
