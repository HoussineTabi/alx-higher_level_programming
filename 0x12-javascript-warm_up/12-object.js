#!/usr/bin/node
/*
Script that searches for the second biggest of a list

all the arguments can be converted to integers
*/
const myObject = {
	type: 'object',
	value: 12
};
console.log(myObject);
myObject.value = 89;
console.log(myObject);
